return {
	'okuuva/auto-save.nvim',
	opts = {}, -- annoying with Oil?
	-- save less often (e.g. 3s) so it doesn't format while editing?
	-- trigger save before closing a buffer so it doesn't complain?
}
